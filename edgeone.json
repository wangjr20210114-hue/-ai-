{
  "buildCommand": "cd frontend && npm ci && npm run build -- --mode edgeone",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "cloudFunctions": {
    "nodejs": {
      "includeFiles": []
    }
  },
  "agents": {
    "framework": "langgraph",
    "timeout": 1800
  }
}
