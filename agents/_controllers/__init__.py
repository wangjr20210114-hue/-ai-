"""Application controllers for EdgeOne Agent route adapters.

Route ``index.py`` modules should authenticate, parse transport input and then
delegate here. Controllers may orchestrate Models and Views, but must not own
provider storage schemas or render frontend UI.
"""
