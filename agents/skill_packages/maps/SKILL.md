---
name: maps
description: 核实真实地点、读取经授权的当前位置，并展示腾讯地图路线和地点卡片。
---

# Maps

直接询问当前位置使用 `needs_current_location`；周边商家使用 `needs_nearby_places`；目的地介绍或多地点推荐使用地点搜索和地图动作；真实道路距离、耗时、费用或有序停靠使用路线能力。

路线停靠点必须按用户原顺序保留，错字和同名解析交给腾讯地点服务；不得在规划器中纠错、改名或选择分店。只有用户明确优先最短耗时才使用 `least_time`。浏览器位置必须由用户授权，坐标和结果只在当前租户范围内使用。
