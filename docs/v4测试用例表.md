# 元宝主动式 Agent v4.1.0 — 详细测试用例表

> 分支：`agent/proactive-runtime-refactor-v2`（含 UX 改进）
> 日期：2026-07-12
> 前端地址：http://127.0.0.1:5173
> 后端地址：http://127.0.0.1:8000
> 访问令牌：`<YOUR_AGENT_TOKEN>`（请求头 `X-Agent-Token`）

---

## 一、基础环境验证（5 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 1 | 后端健康检查 | 浏览器打开 http://127.0.0.1:8000/api/setup/access-token | 返回 JSON，含 `"token"` 字段 |
| 2 | 系统健康 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/system/health` | `status: ok`，supervisor `running: true`，model `status: ready` |
| 3 | 前端加载 | 浏览器打开 http://127.0.0.1:5173 | 页面正常加载，显示聊天界面，左侧有"我的"面板 |
| 4 | 身份恢复 | 刷新页面 | 不要求重新登录，历史消息恢复 |
| 5 | 无 Token 拦截 | `curl http://127.0.0.1:8000/api/bootstrap` | 返回 401 `"local access token required"` |

---

## 二、聊天对话（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 6 | 普通聊天 | 输入"你好，介绍一下你自己" | 显示"思考中"→流式输出回复→完成后显示 3 条追问 |
| 7 | 多轮上下文 | 先问"杭州有什么好玩的"，再问"那里天气怎么样" | 第二轮理解"那里"指杭州，回答关于杭州天气 |
| 8 | Markdown 渲染 | 输入"用 Markdown 格式写一个简单的技术方案" | 回复中有标题、列表、代码块等 Markdown 格式正确渲染 |
| 9 | 刷新后恢复 | 完成一次聊天后刷新页面 | 历史消息完整恢复，包括 AI 回复内容 |

---

## 三、搜索（3 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 10 | 聚合搜索 | 输入"搜索一下最近的 AI Agent 发展" | ① 显示"搜索中"状态 ② 搜索来源面板出现（网页/百科等来源标签）③ AI 流式总结 ④ 总结中有引用编号 |
| 11 | 搜索零结果 | 输入"搜索 asdfqwertyzzz" | 返回"没有找到相关搜索结果"，不崩溃 |
| 12 | 搜索结果恢复 | 完成一次搜索后刷新页面 | 搜索结果面板和 AI 总结都恢复 |

---

## 四、生图（3 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 13 | 用户请求生图 | 输入"帮我画一只可爱的猫咪" | ① 显示"生成中"状态 ② 直接执行，无需确认 ③ 返回图片并在聊天中展示 |
| 14 | 生图结果恢复 | 完成生图后刷新页面 | 图片在聊天中仍可见 |
| 15 | 生图幂等 | 对同一条消息不重复生成（无法手动触发，验证 ActionExecutor 幂等机制） | 同一 prompt 的重复请求返回同一图片，不重复调用混元 |

---

## 五、腾讯会议（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 16 | 会议状态检查 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/meeting/status` | 返回 tmeet CLI 安装/授权状态 |
| 17 | 用户请求创建会议 | 输入"帮我创建一个明天下午 3 点的会议" | ① 识别会议意图 ② 自动提取主题和时间 ③ 通过 ActionExecutor 直接执行 ④ 返回会议号和加入链接 |
| 18 | 会议参数缺失 | 输入"帮我创建一个会议"（不指定时间） | 返回自然语言提示，要求补充时间信息 |
| 19 | 未授权会议 | 如果 tmeet 未授权：输入"创建一个会议" | 返回 `setup_required` 引导，不返回 500 错误 |

---

## 六、旅游规划（5 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 20 | 旅游意图引导 | 输入"我想去旅游" | 返回自然引导，询问目的地、出发地、时间等 |
| 21 | 上下文感知旅游 | 输入"我想去杭州玩两天" | 回复包含目的地"杭州"，不再追问已知信息，只追问缺失项 |
| 22 | 生成旅游计划 | 在旅游建议卡中点击"规划行程"按钮 | 弹出旅游规划助手，收集信息后生成行程 Markdown |
| 23 | 保存旅游计划 | 在旅游规划结果中保存 | 计划出现在"我的"面板的旅游计划列表中 |
| 24 | 旅游计划恢复 | 保存计划后刷新页面 | 计划仍在列表中 |

---

## 七、论文搜索与阅读（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 25 | 论文搜索 | 输入"帮我找一些关于大语言模型的论文" | ① arXiv 搜索 ② AI 流式介绍论文 ③ 论文列表卡片出现（含标题、arXiv ID、摘要） |
| 26 | 论文下载阅读 | 在论文列表中点击某篇论文的"下载"或"阅读" | PDF 下载到文件中心，打开论文阅读器 |
| 27 | 论文恢复 | 搜索论文后刷新页面 | 论文列表和 AI 介绍恢复 |
| 28 | 我的阅读列表 | 打开"我的"面板，查看"我的阅读" | 已保存的论文出现在列表中，可点击阅读 |

---

## 八、翻译（2 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 29 | 自动翻译 | 输入"Translate this to Chinese: The quick brown fox jumps over the lazy dog" | 自动检测外语并翻译为中文，流式输出 |
| 30 | 中文不翻译 | 输入"今天天气真好" | 识别为中文，不触发翻译，走聊天流程 |

---

## 九、日程管理（5 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 31 | 创建日程 | 在"我的"面板点击"+"添加日程：标题"开会"，时间明天 10:00 | 日程出现在列表和日历视图中 |
| 32 | 日程冲突检测 | 再创建一个时间重叠的日程 | 返回冲突提示 |
| 33 | 完成日程 | 点击日程的"完成"按钮 | 日程标记为已完成（划线/灰显） |
| 34 | 删除日程 | 点击日程的"删除"按钮 | 日程从列表消失 |
| 35 | 日程恢复 | 创建日程后刷新页面 | 日程仍在列表中 |

---

## 十、Agent 运行时（5 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 36 | 查看运行记录 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/runs` | 返回 Run 列表，每条含 run_id、status、intent |
| 37 | 查看待确认动作 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/actions` | 返回 awaiting_confirmation 状态的 Action 列表 |
| 38 | 重试失败运行 | 先制造一个失败（如断网时发送消息），再调 `POST /api/runs/{run_id}/retry` | Run 重新进入 queued，Executor 重新执行 |
| 39 | 取消运行 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/runs/{run_id}/cancel` | Run 状态变为 cancelled |
| 40 | 运行详情 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/runs/{run_id}` | 返回 Run 的完整信息，含 observations 审计记录 |

---

## 十一、通知中心（5 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 41 | 查看通知列表 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/notifications` | 返回通知列表，含 title、body、trigger_reason、created_at |
| 42 | 标记已读 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/notifications/{id}/read` | 通知 `read_at` 被设置 |
| 43 | 忽略通知 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/notifications/{id}/dismiss` | 通知 `dismissed_at` 被设置 |
| 44 | 延后通知 | `curl -X POST -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"snooze_until":"2026-07-13T09:00"}' http://127.0.0.1:8000/api/notifications/{id}/snooze` | 通知 `snoozed_until` 被设置 |
| 45 | 通知偏好设置 | `curl -X PUT -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"quiet_hours_start":"23:00","quiet_hours_end":"07:00","daily_limit":3}' http://127.0.0.1:8000/api/notification-preferences` | 设置保存成功，影响后续通知策略 |

---

## 十二、主动调度（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 46 | 查看定时任务 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/scheduled-jobs` | 返回 2 个默认 Job（schedule_collector + travel_weather_collector） |
| 47 | 暂停定时任务 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/scheduled-jobs/{job_id}/pause` | Job 状态变为 paused |
| 48 | 恢复定时任务 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/scheduled-jobs/{job_id}/resume` | Job 状态恢复为 enabled |
| 49 | 日程到期提醒 | 创建一个 5 分钟后到期的日程，等待 Collector 扫描 | 到期后产生通知，出现在通知列表中 |

---

## 十三、长期记忆（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 50 | 创建记忆 | `curl -X POST -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"key":"home_city","value":"杭州","kind":"preference"}' http://127.0.0.1:8000/api/memories` | 返回记忆记录，含 id、version |
| 51 | 列出记忆 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/memories` | 返回记忆列表 |
| 52 | 删除记忆 | `curl -X DELETE -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/memories/home_city` | 返回 `deleted: true` |
| 53 | 更新记忆 | 再次 POST 同一 key 但不同 value | version 递增，value 更新 |

---

## 十四、用户反馈（2 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 54 | 记录反馈 | `curl -X POST -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"run_id":"","feedback_action":"dismissed","reason":"不需要"}' http://127.0.0.1:8000/api/feedback` | 返回 `status: recorded` |
| 55 | 查看反馈历史 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/feedback` | 返回反馈记录列表 |

---

## 十五、用量与成本（2 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 56 | 日用量 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/usage/daily` | 返回当天 token 数、调用次数、估算成本 |
| 57 | Run 用量 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/usage/runs/{run_id}` | 返回该 Run 的用量明细 |

---

## 十六、系统管理（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 58 | 数据导出 | `curl -X POST -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/system/backup/export` | 返回备份文件路径或 JSON 数据包 |
| 59 | 数据恢复 | `curl -X POST -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"backup_data":{}}' http://127.0.0.1:8000/api/system/backup/restore` | 返回恢复结果（不覆盖 .env 和 token） |
| 60 | 会话列表 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/conversations` | 返回会话列表 |
| 61 | 创建新会话 | `curl -X POST -H "X-Agent-Token: <token>" -H "Content-Type: application/json" -d '{"title":"测试会话"}' http://127.0.0.1:8000/api/conversations` | 返回新会话 ID |

---

## 十七、文件管理（3 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 62 | 列出文件 | `curl -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/files` | 返回文件列表 |
| 63 | 上传文件 | `curl -X POST -H "X-Agent-Token: <token>" -F "file=@test.pdf" http://127.0.0.1:8000/api/files/upload` | 返回 file_id，文件存入统一文件中心 |
| 64 | 删除文件 | `curl -X DELETE -H "X-Agent-Token: <token>" http://127.0.0.1:8000/api/files/{file_id}` | 返回 `deleted: true` |

---

## 十八、边界与异常（4 项）

| # | 用例 | 操作步骤 | 预期结果 |
|---|------|---------|---------|
| 65 | 空消息 | 在聊天框输入空内容发送 | 不触发任何操作 |
| 66 | 超长输入 | 输入超过 10000 字的文本 | 正常处理，不崩溃 |
| 67 | 快速连续发送 | 连续快速发送 3 条消息 | 每条都被处理，不丢失 |
| 68 | 断线重连 | 发送消息后断开 WiFi，等待 5 秒后恢复 | WebSocket 自动重连，如果期间有通知则补发 |

---

## 测试优先级

| 优先级 | 用例编号 | 说明 |
|--------|---------|------|
| **P0 必测** | 1, 3, 6, 10, 13, 17, 20, 25, 29, 31 | 核心功能路径 |
| **P1 重要** | 4, 9, 14, 21, 22, 26, 27, 35, 36, 37, 41, 49 | 持久化和恢复 |
| **P2 验证** | 其余 | API 细节和边界 |

---

## 测试命令速查

```bash
TOKEN="<YOUR_AGENT_TOKEN>"
BASE="http://127.0.0.1:8000/api"

# 健康检查
curl -s -H "X-Agent-Token: $TOKEN" $BASE/system/health | python3 -m json.tool

# 引导
curl -s -H "X-Agent-Token: $TOKEN" $BASE/bootstrap | python3 -m json.tool

# 运行记录
curl -s -H "X-Agent-Token: $TOKEN" $BASE/runs | python3 -m json.tool

# 待确认动作
curl -s -H "X-Agent-Token: $TOKEN" $BASE/actions | python3 -m json.tool

# 通知
curl -s -H "X-Agent-Token: $TOKEN" $BASE/notifications | python3 -m json.tool

# 定时任务
curl -s -H "X-Agent-Token: $TOKEN" $BASE/scheduled-jobs | python3 -m json.tool

# 记忆
curl -s -H "X-Agent-Token: $TOKEN" $BASE/memories | python3 -m json.tool

# 日用量
curl -s -H "X-Agent-Token: $TOKEN" $BASE/usage/daily | python3 -m json.tool

# 反馈
curl -s -H "X-Agent-Token: $TOKEN" $BASE/feedback | python3 -m json.tool
```
